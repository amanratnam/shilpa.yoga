/**
 * Shared admin scale. The marketing site sets its own rhythm; these keep the
 * CRM screens denser and quieter than the public pages, and consistent with
 * each other rather than each page inventing its own label.
 */

/** Column headers and definition-list labels. */
export const META_LABEL =
  "text-[0.6875rem] font-medium uppercase tracking-[0.06em] text-brand-stone";

/** Table cell padding — dense enough to scan many rows at once. */
export const CELL = "px-5 py-3.5";
